<%= form_for(@categories_of_certificate_type) do |f| %>
  <% if @categories_of_certificate_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categories_of_certificate_type.errors.count, "error") %> prohibited this categories_of_certificate_type from being saved:</h2>

      <ul>
      <% @categories_of_certificate_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
